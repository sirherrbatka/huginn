(cl:in-package #:huginn.machine.operations)


(defun clause-to-heap (clause heap stack)
  "Will attempt to place clause on the heap. Will check if operation can be performed. Will return new stack-cell if succedes or the STACK if not."
  cl-ds.utils:todo)
